[package]
name = "swiftui_ast"
version = "0.1.0"
edition = "2024"

[dependencies]
ui_spec = { path = "../ui_spec" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"
