[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
orchestrator = { path = "../orchestrator" }
url = "2.5"

[dev-dependencies]
serde_json = "1.0"
