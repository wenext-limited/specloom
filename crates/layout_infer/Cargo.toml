[package]
name = "layout_infer"
version = "0.1.0"
edition = "2024"

[dependencies]
figma_normalizer = { path = "../figma_normalizer" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
